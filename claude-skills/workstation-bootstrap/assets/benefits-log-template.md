# Benefits Log

Purpose: a quantitative record of what the rules-vs-facts method actually buys in this project — token efficiency, usage patterns, and contextual-understanding quality — so any claims about the method rest on real sessions, not impressions.

## How to log an entry

After a session where something notable happened — a big token difference, a moment Claude correctly applied a project rule without being told, a moment it *didn't* and you had to correct it — do this:

1. If a usage-breakdown tool or skill is available for the session, use it to get token figures.
2. Add one row to the table below. Keep it short — 1–2 sentences per column, same hygiene as the memory system.

Don't force an entry every session. Sparse-but-real data beats padded data.

## Entries

| Date | Session / Task | Tokens (if measurable) | Context-quality note | Before/After comparison |
|---|---|---|---|---|
| — | — | — | — | — |

## Summary (update periodically once entries accumulate)

Once there are enough rows to see a pattern, summarize it here in 2–3 sentences: rough token trend, most common type of context win, anything that *didn't* work as expected. Quote this section — not the raw table — when describing the method's value elsewhere.
